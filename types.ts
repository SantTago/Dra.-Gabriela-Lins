import { LucideIcon } from 'lucide-react';

export interface TreatmentProps {
  icon: LucideIcon;
  title: string;
  description: string;
}

export interface NavItem {
  label: string;
  href: string;
}